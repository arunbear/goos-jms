spring:
  application:
    name: goos-jms
  artemis:
    mode: embedded
    embedded:
      enabled: true
      persistent: false

messaging:
  auction:
    queue: queue-auction-house
  sniper:
    queue: queue-sniper
    id: sniper-1

logging:
  level:
    org.apache.activemq.audit: OFF
